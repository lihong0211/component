import FlyAnimateNumber from './index'

/* istanbul ignore next */
FlyAnimateNumber.install = function (Vue) {
  Vue.component(FlyAnimateNumber.name, FlyAnimateNumber)
}

export default FlyAnimateNumber
